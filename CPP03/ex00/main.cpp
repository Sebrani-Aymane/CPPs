#include "ClapTrap.hpp"

int main()
{
    ClapTrap clptrp("Claptrap");
    ClapTrap b (clptrp);
    // clptrp.attack("Enemy");
    // clptrp.takeDamage(5);
    // clptrp.beRepaired(2);
    // clptrp.takeDamage(100);
    // clptrp.beRepaired(10);
    // clptrp.attack("Enemy");
    
}
